package homeTask20240111.pattern.connection;

public class FastConnection extends Connection{
    public FastConnection(int id, String host, int port, String protocol) {
        super(id, host, port, protocol);
    }
}
